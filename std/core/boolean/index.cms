export function Boolean(v: string): boolean {
	return v.length > 0;
}

// NaN is the one double that is falsy despite being non-zero.
export function Boolean(v: float64): boolean {
	return v !== 0.0 && v === v;
}

export function Boolean(v: boolean): boolean {
	return v;
}
