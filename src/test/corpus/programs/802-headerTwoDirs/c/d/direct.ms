import { viaThrice } from "../../a/b/user";
import { corpus802Thrice } from "../../a/b/thrice.h";

export function thriceBoth(x: int32): string { return `${viaThrice(x)},${corpus802Thrice(x + 1)}`; }
